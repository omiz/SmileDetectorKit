import XCTest
@testable import SmileDetectorKit

final class SmileDetectorKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SmileDetectorKit().text, "Hello, World!")
    }
}
